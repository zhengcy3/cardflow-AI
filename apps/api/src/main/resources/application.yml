server:
  port: 8080

spring:
  datasource:
    url: jdbc:sqlite:data/cardflow.db
    driver-class-name: org.sqlite.JDBC
  ai:
    openai:
      base-url: https://api.deepseek.com
      api-key: sk-35d6529874d6471090cfef9998ba07ee
      chat:
        options:
          model: deepseek-chat
          temperature: 0.85

cardflow:
  storage:
    output-dir: storage/outputs
  app:
    default-user-id: local-user